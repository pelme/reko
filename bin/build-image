#! /usr/bin/env bash

set -euo pipefail

PROJECT_ROOT="$(realpath $(dirname $0)/../)"
cd "$PROJECT_ROOT"

rm -rf dist
mkdir dist

cp Dockerfile dist
uv run --only-group docs sphinx-build docs dist/_build
uv build --wheel

docker build -t "ghcr.io/pelme/reko:$TAG" dist
